import './LoginPage.css';
import { LoginForm } from '../../features';

const LoginPage = () => {
    return (
        <div className="">
            <LoginForm />
        </div>
    );
};

export default LoginPage;
